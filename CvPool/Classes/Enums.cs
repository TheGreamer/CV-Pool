namespace CvPool.Classes
{
    public enum VisibilityProcess { ShowSingle, HideAll, SetDocks }

    public enum JoinType { Inner, Left, Right, Full }

    public enum ArithmeticOperator { Equal, NotEqual, LessThan, LessThanOrEqual, BiggerThan, BiggerThanOrEqual }

    public enum LogicOperator { And, Or }
}